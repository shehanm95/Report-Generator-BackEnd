spring:
  application:
    name: report-service
  datasource:
    url: jdbc:mysql://localhost:3306/reportGen?createDatabaseIfNotExist=true
    username: root
    password: 12345

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true